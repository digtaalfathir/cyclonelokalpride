'use strict';

/**
 * shared/workflow — Workflow parsing, serialization, and validation utilities.
 *
 * Placeholder for future extraction. Planned modules:
 *   - parser.js    : parse raw JSON into validated workflow graph
 *   - serializer.js: serialize in-memory workflow to storable format
 *   - validator.js : schema validation, cycle detection, missing-node checks
 */

module.exports = {};
